# Ambiente usado apenas pelos testes automatizados.
# Banco separado para os testes nunca apagarem os dados de desenvolvimento.
# Nao ha segredo real aqui, por isso este arquivo pode ser versionado.

DATABASE_URL="file:./test.db"
JWT_SECRET="segredo-apenas-para-testes-nao-usar-em-producao"
JWT_EXPIRES_IN="1d"
